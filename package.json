{
  "name": "@nichifor-dragos/hotel-management-root",
  "scripts": {
    ".": "bash .nx-run-auto.sh",
    "start": "pnpm -s . start",
    "clean": "pnpm -s . clean",
    "lint": "pnpm -s . lint",
    "format": "pnpm -s . format",
    "build": "pnpm -s . build",
    "pre-commit": "lint-staged -r",
    "backend:new-migration": "pnpm -s . backend create-migration",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.2.2",
    "nx": "17.1.3"
  }
}
